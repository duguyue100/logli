Hello, Logli! 欢迎光临。

This is a sample post to prove the pipeline. You can drop any `.md` file in `_posts/` — no date in the filename needed, no front matter required. The build assigns each post its date from the first git commit that added it.

Math works via KaTeX:

$$e^{i\pi} + 1 = 0$$

And inline like $x^2 + y^2 = z^2$.

长文中文内容在这里显示，字体使用仿宋。

> A blockquote, because they exist.

    And a code block.

- Cards show all content. No truncation, no comments, no likes.
- Click **Share** on a card to copy a paste-ready HTML snippet.
- Use the search box and the sort button in the top bar.